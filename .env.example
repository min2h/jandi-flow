# Local only. Copy to .env and fill in. Never commit real values.
JANDI_SECRET=
PORT=8787
HOST=127.0.0.1
TZ=Asia/Seoul

# Required if HOST is not 127.0.0.1, and for a public UI behind nginx.
JANDI_UI_USER=
JANDI_UI_PASS=
